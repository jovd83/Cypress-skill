interface:
  display_name: "Cypress Pom"
  short_description: "Page Object Model patterns for Cypress - when to use POM"
  default_prompt: "Use $cypress-pom to help with page Object Model patterns for Cypress - when to use POM."

policy:
  allow_implicit_invocation: true
