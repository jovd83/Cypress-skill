interface:
  display_name: "Cypress Orchestrator"
  short_description: "Central entrypoint for broad or ambiguous Cypress requests"
  default_prompt: "Use $cypress-orchestrator to help with central entrypoint for broad or ambiguous Cypress requests."

policy:
  allow_implicit_invocation: true
