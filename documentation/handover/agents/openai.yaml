interface:
  display_name: "Cypress Handover Compat"
  short_description: "need the legacy Cypress handover entrypoint path"
  default_prompt: "Use $cypress-handover-compat to help when you need the legacy Cypress handover entrypoint path."

policy:
  allow_implicit_invocation: true
