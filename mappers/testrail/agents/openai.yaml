interface:
  display_name: "Cypress Mapper Testrail"
  short_description: "map TestRail unique IDs back to local Cypress test"
  default_prompt: "Use $cypress-mapper-testrail to map TestRail unique IDs back to local Cypress test documentation."

policy:
  allow_implicit_invocation: true
