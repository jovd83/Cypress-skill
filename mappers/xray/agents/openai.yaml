interface:
  display_name: "Cypress Mapper Xray"
  short_description: "map Xray unique IDs back to local Cypress test documentation"
  default_prompt: "Use $cypress-mapper-xray to map Xray unique IDs back to local Cypress test documentation."

policy:
  allow_implicit_invocation: true
