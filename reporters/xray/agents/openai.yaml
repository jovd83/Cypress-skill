interface:
  display_name: "Cypress Reporter Xray"
  short_description: "use Xray APIs to report Cypress test execution results"
  default_prompt: "Use $cypress-reporter-xray to use Xray APIs to report Cypress test execution results."

policy:
  allow_implicit_invocation: true
