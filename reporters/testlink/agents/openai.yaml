interface:
  display_name: "Cypress Reporter Testlink"
  short_description: "use TestLink APIs to report Cypress test execution results"
  default_prompt: "Use $cypress-reporter-testlink to use TestLink APIs to report Cypress test execution results."

policy:
  allow_implicit_invocation: true
