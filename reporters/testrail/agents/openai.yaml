interface:
  display_name: "Cypress Reporter Testrail"
  short_description: "use TestRail APIs to report Cypress test execution results"
  default_prompt: "Use $cypress-reporter-testrail to use TestRail APIs to report Cypress test execution results."

policy:
  allow_implicit_invocation: true
