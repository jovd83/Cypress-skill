interface:
  display_name: "Cypress Transformer Xray"
  short_description: "transform Cypress natural language test cases into Xray"
  default_prompt: "Use $cypress-transformer-xray to transform Cypress natural language test cases into Xray compatible import formats (JSON/CSV)."

policy:
  allow_implicit_invocation: true
