interface:
  display_name: "Cypress Transformer Testrail"
  short_description: "transform Cypress natural language test cases into TestRail"
  default_prompt: "Use $cypress-transformer-testrail to transform Cypress natural language test cases into TestRail compatible import formats (XML/CSV)."

policy:
  allow_implicit_invocation: true
