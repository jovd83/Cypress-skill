interface:
  display_name: "Cypress Migration"
  short_description: "Migration skill for moving existing UI automation to Cypress"
  default_prompt: "Use $cypress-migration to help with migration skill for moving existing UI automation to Cypress."

policy:
  allow_implicit_invocation: true
